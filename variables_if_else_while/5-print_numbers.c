#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char number
	number = 0;
	while (number < 10);
	putchar ("%d\n",number);
		number++;


	return (0);
}
