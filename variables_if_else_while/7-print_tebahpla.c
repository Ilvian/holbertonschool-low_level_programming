#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Alph;

	for (Alph = 122; Alph >= 97; Alph--)
		putchar(Alph);
			putchar('\n');

	return (0);
}
