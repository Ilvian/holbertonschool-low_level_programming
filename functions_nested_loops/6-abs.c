#include "main.h"
/**
 * _abs - it takes an argument of type in and returns its anbosulte value
 * @n: integer
 * Return: the absolute value of n
 */
int _abs(int n)
{

	if (n >= 0)
		return (n);
			else
				return (-n);
}
